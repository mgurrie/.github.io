d3.csv("assets/csv/studentData.csv", function(tableData){

	console.log(tableData)

  //var parsedCSV = d3.csvParseRows(tableData);
	//var parsedCsv = d3.dsvFormat(",").parseRows(tableData)

	/*
  var container = d3.select("#progress_table_div")
    .append("table")

    .selectAll("tr")
        .data(parsedCSV).enter()
        .append("tr")

    .selectAll("td")
        .data(function(d) {return d;}).enter()
        .append("td")
        .text(function(d) { return d; });
	*/
        
})